# Dev tooling and scratch files — keep these out of the production deploy
shot.mjs
temporary screenshots/
*.pdf

# GIFs are never served — the site uses .webm/.mp4 twins instead (see CLAUDE.md).
# Backstop so a stray GIF can't re-bloat the deploy.
*.gif
